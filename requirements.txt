# Pinned <2.0: 2.x drops mcp.server.fastmcp, and state.serialize_per_presentation
# patches the 1.x _tool_manager._tools registry. See CLAUDE.md before widening.
mcp[cli]>=1.8.0,<2.0.0
python-pptx
Pillow
fonttools
httpx>=0.27.0
pymupdf>=1.28.2
python-dotenv>=1.2.3
# Already a transitive dependency of mcp; declared directly because
# state.serialize_per_presentation dispatches tool calls to a worker thread
# pool via anyio.to_thread.run_sync/CapacityLimiter. See CLAUDE.md.
anyio>=4.0.0
